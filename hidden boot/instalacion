ATAKE BOOT 

Este ataque esta destinado a localizar informacio del sistema se basa en la descripcion y 
se utiliza para dezanonimizacion una ves detectado utilizar el exploit correcto

DISTRIB_ID
DISTRIB_RELEASE
DISTRIB_CODENAME
DISTRIB_DESCRIPTION

FIXED BUG RANDOMIZE

Verificar tu Boot IDS
cat /proc/sys/kernel/random/boot_id

Instalacion / Mueve los archivos a sus carpetas correspondientes
y despues abrir terminal . 

sudo chmod +x /etc/init.d/bootid
sudo update-rc.d bootid defaults 10
sudo update-rc.d bootid defaults 10
sudo update-rc.d bootid defaults 10
sudo update-rc.d bootid defaults 10
sudo update-rc.d bootid defaults 10

sudo chmod +x /etc/init.d/bootxid
sudo update-rc.d bootxid defaults 10
sudo update-rc.d bootxid defaults 10
sudo update-rc.d bootxid defaults 10
sudo update-rc.d bootxid defaults 10
sudo update-rc.d bootxid defaults 10

sudo /etc/init.d/bootid start
sudo /etc/init.d/bootxid start

Verificar Nuevamente
cat /proc/sys/kernel/random/boot_id








